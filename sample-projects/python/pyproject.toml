[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "chrono-python-demo"
version = "1.0.0"
description = "CHRONOVAULT demo project — Python"
requires-python = ">=3.9"